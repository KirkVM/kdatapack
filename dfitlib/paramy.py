#from typing import 


#def get_fit_params(callable fitfunc,indepdata (NbyD),depend_data(N))
#def get_fit_params(callable fitfunc,indepdata (NbyD),depend_data(N))
'''takes a callable function, array of size N(#datapoints) x D (#dimensions),
    and  dependent array of length N.

    returns: array of optimized parameters
'''
#options to add: 
#(1) minimization: l1 (self-rolled) or l2 (lmfit? sklearn? statsmodels?)
#(2) uncertainties: least-squares minimized values or bootstrap or emcee or 
#(3) return residuals?

#need separate for linear vs nonlinear?

